# autoscholar installation notes::

## install mongodb

## install libxml

## install custom dependencies
pip install numpy
pip install -r /ext/sciscrape/requirements.txt
pip install selenium
pip install pymongo
(cd ext/sciscrape && python setup.py install)

## chrome / selenium setup
install chrome
download chromedriver and add to path